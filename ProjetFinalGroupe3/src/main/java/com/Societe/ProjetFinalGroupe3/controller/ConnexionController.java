package com.Societe.ProjetFinalGroupe3.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.CrossOrigin;
import org.springframework.web.bind.annotation.RestController;

import com.Societe.ProjetFinalGroupe3.metier.Utilisateur;

@CrossOrigin
@RestController
public class ConnexionController {
	/*Utilisateur u = new Utilisateur();
	@Autowired
	public String checkPassword() {
		u = service.authentification(login, mdp);
		
		if (u.getLogin() != null && u.getMotdepasse() != null && u.getIdAdministrateur() == 1) {
		
			
			request.getRequestDispatcher("./pageGerant.jsp").forward(request, response);
			
		} else if (u.getLogin() != null && u.getMotdepasse() != null && u.getIdAdherent() != 1) {
		
			
			
		} else {
			request.getRequestDispatcher("./index.jsp").forward(request, response);
		}
		;
		
		
		
		
		return "";
		
	}*/
	
	
	
}
